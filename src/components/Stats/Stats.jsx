import React from 'react';
//import PropTypes from 'prop-types';
import './Stats.scss';

function Stats() {
    return (
        <div className="stats">
          
        </div>
    );
}
  
Stats.propTypes = {
};

Stats.defaultProps = {
};

export default React.memo(Stats);